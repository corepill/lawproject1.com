<div class="mb-5">
    <h2 class="md:text-4xl text-3xl md:leading-10 font-medium text-gray-800">{{ $heading }}</h2>
    
    @if ($subtext)
        <p class="text-gray-600 mt-6">{{ $subtext }}</p>
    @endif
</div>
